<div class="p-3">
  <div class="relative">
    <input type="text" placeholder="搜尋成員" class="bg-gray-900 w-full text-sm px-2 py-1 rounded focus:outline-none focus:ring-1 focus:ring-blue-500">
    <i class="fas fa-search absolute right-2 top-1.5 text-xs text-gray-400"></i>
  </div>
</div>
<%= turbo_stream_from "online_users" %>
<!-- 用戶列表側邊欄 -->
<div class="bg-gray-800 w-60 flex-shrink-0 overflow-y-auto scrollbar-thin" id="online-status">
  <!-- 線上狀態組 -->
  <div class="px-3 mb-2">
    <div class="text-xs font-semibold text-gray-400 mb-2 flex items-center">
      <span>線上 — <%= User.online.count %></span>
      <i class="fas fa-chevron-down ml-1"></i>
    </div>

    <% User.online.each do |user| %>
      <div class="py-1 flex items-center hover:bg-gray-700 rounded px-2 cursor-pointer">
        <div class="w-8 h-8 rounded-full bg-green-600 flex items-center justify-center relative">
          <span class="font-medium text-sm">XD</span>
          <div class="absolute bottom-0 right-0 w-3 h-3 bg-green-400 rounded-full border-2 border-gray-800"></div>
        </div>
        <span class="ml-2 text-gray-300 text-sm"><%=user.username%></span>
        <% if user == current_user%>
          <span class="ml-1 text-xs text-gray-400">（你）</span>
        <% end %>
      </div>
    <% end %>
  </div>
  <!-- 離線狀態組 -->
  <div class="px-3">
    <div class="text-xs font-semibold text-gray-400 mb-2 flex items-center">
      <span>離線 — <%=User.offline.count%></span>
      <i class="fas fa-chevron-down ml-1"></i>
    </div>

    <% User.offline.each do |user| %>
      <div class="py-1 flex items-center hover:bg-gray-700 rounded px-2 cursor-pointer">
        <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center relative opacity-70">
          <span class="font-medium text-sm">OX</span>
          <div class="absolute bottom-0 right-0 w-3 h-3 bg-gray-500 rounded-full border-2 border-gray-800"></div>
        </div>
        <span class="ml-2 text-gray-500 text-sm"><%=user.username%></span>
      </div>
    <% end %>
  </div>
</div>
